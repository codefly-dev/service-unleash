# Unleash service

The generated local secret configuration starts a development instance. Replace every generated value before sharing the environment.

The admin endpoint is module-visible. Edge is the only endpoint declared public.
